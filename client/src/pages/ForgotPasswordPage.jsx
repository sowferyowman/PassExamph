import { useState } from "react";
import { Link } from "react-router-dom";
import { forgotPassword } from "../api/authApi";

export default function ForgotPasswordPage() { const [email, setEmail] = useState(""); const [message, setMessage] = useState(""); async function submit(event) { event.preventDefault(); const result = await forgotPassword(email).catch(() => ({ message: "If that account exists, reset instructions were created." })); setMessage(result.message); } return <main className="grid min-h-screen place-items-center bg-slate-950 p-6"><form onSubmit={submit} className="w-full max-w-md space-y-4 rounded-2xl bg-white p-8 shadow-2xl"><h1 className="text-2xl font-black text-slate-950">Forgot Password</h1><p className="text-sm text-slate-500">Enter your email to request a secure reset token.</p><input required type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="you@example.com" className="w-full rounded-lg border border-slate-200 px-3 py-3" /><button className="w-full rounded-lg bg-blue-700 py-3 font-black text-white">Request Reset</button>{message && <p className="text-sm font-semibold text-emerald-700">{message}</p>}<Link to="/login" className="block text-center text-sm font-bold text-blue-700">Back to login</Link></form></main>; }
